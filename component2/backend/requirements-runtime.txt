# Lighter install for the interview API only (no pandas/torch/SBERT build chain).
# Use when full `requirements.txt` fails on Python 3.13+ without VS Build Tools.
# Install: python -m pip install -r requirements-runtime.txt
#
# For ML features (SBERT descriptive scoring, embedding-based RAG), use Python 3.12
# and install full requirements.txt instead.

fastapi==0.109.0
uvicorn==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0
python-multipart==0.0.6
python-dotenv==1.0.0
sqlalchemy==2.0.23
pymongo==4.6.0
motor==3.3.2
requests==2.31.0
aiofiles==23.2.1
cors==1.0.1
python-jose==3.3.0
passlib==1.7.4
bcrypt==4.1.1
jwt==1.3.1
